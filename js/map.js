class Mymap {
    constructor() {
        /*this.createMap();
        this.showUser();
        this.showRestaurant(tabRestaurants);*/
        
    }


    createMap() {

    }
    
    /*showRestaurant(tabRestaurants) {
        
        for (const tabRestaurant of tabRestaurants) {

                $(`<div class="${tabRestaurant.name}"></div>`).appendTo('.restaurants');
                $(`<h2>${tabRestaurant.name}</h2>`).appendTo('.' + tabRestaurant.name);
                $(`<p>${tabRestaurant.address}</p>`).appendTo('.' + tabRestaurant.name);
                $(`<p>${tabRestaurant.location.latitude}</p>`).appendTo('.' + tabRestaurant.name);
                $(`<p>${tabRestaurant.location.longitude}</p>`).appendTo('.' + tabRestaurant.name);
            }

    }*/

    showUser() {

    }

}
